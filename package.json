{
  "name": "procurement-system",
  "version": "1.0.0",
  "description": "Procurement & Product Intelligence System - Offline-first PWA",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx wrangler pages deploy dist",
    "deploy-worker": "npx wrangler deploy --config workers/wrangler.toml",
    "r2-create": "npx wrangler r2 bucket create procurement-images"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "idb": "^8.0.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "tailwindcss": "^3.4.0",
    "vite": "^5.0.0",
    "wrangler": "^4.67.0"
  }
}
